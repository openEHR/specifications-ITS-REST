= Simplified Information Model (SIM) and Transformation Rules

== Overview

The Simplified Information Model (SIM) is a version of the canonical Reference Model and Base Model (RM, BASE components respectively) that contains structurally simpler representations of many class attributes, enabling easier construction of string-oriented serialisations such as JSON, compared to the full canonical structures. The SIM is defined as a set of classes named after a sub-set of RM classes, with `S_` prepended; for example, in the SIM, the RM class `ELEMENT` becomes `S_ELEMENT`. 

Various kinds of simpilifications are used in the SIM classes, including:

* String representation of some fields that are structured in the RM, e.g `CODE_PHRASE._terminology_id_` is of type `TERMINOLOGY_ID` but in the SIM, it is a `String`;
* replacing fully expressed `DV_CODED_TEXT` or `CODE_PHRASE` fields that represent codes from openEHR or other fixed (e.g. IANA media type) vocabularies which are determinable from the openEHR RM, with Strings;
* compressing fields of some sub-part objects into a parent object in the simplified version, reducing the need for long paths, e.g. `S_PARTY_PROXY` includes fields from `PARTY_PROXY._external_ref_`, of type `PARTY_REF`.

The cost of doing the above is that some work has to be done to convert an `S_XXX` instance into an `XXX` canonical instance, in some cases, requiring the use of a formal representation of the RM. In openEHR, this is available in the form of a BMM model, as described in the {openehr_lang_latest_bmm}[openEHR Basic-Meta Model specification^]. Additionally, some fields in data have to be parsed with various micro-parsers, e.g. for Date/Time.

The subset of RM classes for which SIM classes are defined is limited to the class `COMPOSITION` and classes reachable from it, or approximately 45 classes. Almost all simplifications are defined in near-leaf classes such as the `S_DATA_VALUE` descendants, `PARTICIPATION` and so on; classes such as `S_ENTRY` and `S_COMPOSITION` are largely structured the same way as in the canonical RM, except that sub-parts are of type `S_XXX` rather than `XXX`, where the latter is an original RM type. The exception to this is replacement of coded fields whose vocabulary is defined in the RM.

TODO: describe transformation rules representation. This could be formalised into the BMM as a new kind of structure, which would allow formal representation in BMM of transformation rules. At the moment, a few example rules are shown below interspersed within class definitions. In the final form, these rules would be removed to a separate section / source file in either BMM, or some formal language.

The specific transformations between SIM and RM instances are expressed in the tables in the Transformation Rules sub-sections below.

== Simplified IM Package

TODO: we may remove the `_to_rm()_` / `_from_from()_` approach and just stick with rules.

The following UML diagram shows the top-level `simplified_im` package defining the special interface type `S_TYPE`.

[.text-center]
.`sm.simplified_im` package
image::{uml_diagrams_uri}/SM-simplified_im.svg[id=sm_simplified_im, align="center"]

=== Class Definitions

include::{uml_export_dir}/classes/s_type.adoc[leveloffset=+1]

== Base Model Package

The following UML diagram shows the SIM `base` package defining `S_XXX` types corresponding to the Base `base` package types.

[.text-center]
.`sm.simplified_im.base` package
image::{uml_diagrams_uri}/SM-simplified_im.base.svg[id=sm_simplified_im_base, align="center"]

=== Class Definitions

include::{uml_export_dir}/classes/s_object_ref.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_object_id.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_generic_id.adoc[leveloffset=+1]

=== Transformation Rules

[cols="1,2,1,1,2,1,2", options="header"]
|===
|RM class|Path|Type|SIM class|Path|Type|Conversion

|`OBJECT_REF`
|`_namespace_`
|`String`
|`S_OBJECT_REF`
|`_id_namespace_`
|`String`
|`_copy()_`

|`OBJECT_REF`
|`_type_`
|`String`
|`S_OBJECT_REF`
|`_id_type_`
|`String`
|`_copy()_`

|===

== RM Data types Package

The following UML diagram shows the SIM `rm.data_types` package defining `S_XXX` types corresponding to the RM `rm.data_types` package types.

[.text-center]
.`sm.simplified_im.rm.data_types` package
image::{uml_diagrams_uri}/SM-simplified_im.rm.data_types.svg[id=sm_simplified_im_rm_data_types, align="center"]

=== Class Definitions

include::{uml_export_dir}/classes/s_data_value.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_dv_identifier.adoc[leveloffset=+1]

include::{uml_export_dir}/classes/s_dv_text.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_dv_coded_text.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_code_phrase.adoc[leveloffset=+1]

include::{uml_export_dir}/classes/s_dv_parsable.adoc[leveloffset=+1]

include::{uml_export_dir}/classes/s_dv_quantity.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_dv_count.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_dv_proportion.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_dv_ordinal.adoc[leveloffset=+1]

=== Transformation Rules

[cols="1,2,1,1,2,1,2", options="header"]
|===
|RM class|Path|Type|SIM class|Path|Type|Conversion

|`DV_TEXT`
|`_value_`
|`String`
|`S_DV_TEXT`
|`_value_`
|`String`
|`_copy()_`

|`DV_CODED_TEXT`
|`_defining_code.code_string_`
|`String`
|`S_DV_CODED_TEXT`
|`_code_`
|`String`
|`_copy()_`

|`DV_CODED_TEXT`
|`_defining_code.terminology_id_`
|`TERMINOLOGY_ID`
|`S_DV_CODED_TEXT`
|`_terminology_`
|`String`
|`_as_string()_`

|`CODE_PHRASE`
|`_code_string_`
|`String`
|`S_CODE_PHRASE`
|`_code_`
|`String`
|`_copy()_`

|`CODE_PHRASE`
|`_terminology_id_`
|`TERMINOLOGY_ID`
|`S_CODE_PHRASE`
|`_terminology_`
|`String`
|`_as_string()_`

|===

== Data Structures Package

The following UML diagram shows the SIM `rm.data_structures` package defining `S_XXX` types corresponding to the RM `rm.data_structures` package types.

[.text-center]
.`sm.simplified_im.rm.data_structures` package
image::{uml_diagrams_uri}/SM-simplified_im.rm.data_structures.svg[id=sm_simplified_im_rm_data_structures, align="center"]

=== Class Definitions

include::{uml_export_dir}/classes/s_history.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_event.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_point_event.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_interval_event.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_item.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_cluster.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_element.adoc[leveloffset=+1]

=== Transformation Rules

[cols="1,2,1,1,2,1,2", options="header"]
|===
|RM class|Path|Type|SIM class|Path|Type|Conversion

|`EVENT`
|`_time_`
|`DV_DATE_TIME`
|`S_EVENT`
|`_value_`
|`String`
|`_as_string()_`

|`EVENT`
|`_data_`
|`ITEM_TREE`
|`S_EVENT`
|`_data_`
|`S_CLUSTER`
|`_xxx()_`

|`EVENT`
|`_state_`
|`ITEM_TREE`
|`S_EVENT`
|`_state_`
|`S_CLUSTER`
|`_xxx()_`

|`INTERVAL_EVENT`
|`_width_`
|`DV_DURATION`
|`S_INTERVAL_EVENT`
|`_width_`
|`String`
|`_as_string()_`

|`INTERVAL_EVENT`
|`_sample_count_`
|`DV_DURATION`
|`S_INTERVAL_EVENT`
|`_sample_count_`
|`String`
|`_copy()_`

|`INTERVAL_EVENT`
|`_math_function_`
|`DV_CODED_TEXT`
|`S_INTERVAL_EVENT`
|`_math_function_`
|`String`
|`_extract_code()_`

|===

== Common Package

The following UML diagram shows the SIM `rm.common` package defining `S_XXX` types corresponding to the RM `rm.common` package types.

[.text-center]
.`sm.simplified_im.rm.common` package
image::{uml_diagrams_uri}/SM-simplified_im.rm.common.svg[id=sm_simplified_im_rm_common, align="center"]

=== Class Definitions

include::{uml_export_dir}/classes/s_locatable.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_archetyped.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_link.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_feeder_audit.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_feeder_audit_details.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_party_proxy.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_party_identified.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_participation.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_participation_identified.adoc[leveloffset=+1]

=== Transformation Rules

[cols="1,2,1,1,2,1,2", options="header"]
|===
|RM class|Path|Type|SIM class|Path|Type|Conversion

|`LINK`
|`_meaning.value_`
|`String`
|`S_LINK`
|`_meaning_`
|`String`
|`_copy()_`

|`LINK`
|`_type.value_`
|`String`
|`S_LINK`
|`_type_`
|`String`
|`_copy()_`

|`PARTY_PROXY`
|`_external_ref.id.value_`
|`String`
|`S_PARTY_PROXY`
|`_id_`
|`String`
|`_xxx()_`

|`PARTY_PROXY`
|`_external_ref.namespace_`
|`String`
|`S_PARTY_PROXY`
|`_id_namespace_`
|`String`
|`_xxx()_`

|`PARTY_PROXY`
|`_external_ref.id.scheme_`
|`String`
|`S_PARTY_PROXY`
|`_id_scheme_`
|`String`
|`if external_ref.id instance_of(GENERIC_ID) then {_xxx()_ }`

|===

== Composition Package

The following UML diagram shows the top-level SIM `rm.composition` package defining `S_COMPOSITION`, `S_SECTION` and the `S_ENTRY` types.

[.text-center]
.`sm.simplified_im.rm.composition` package
image::{uml_diagrams_uri}/SM-simplified_im.rm.composition.svg[id=sm_simplified_im_rm_composition, align="center"]

=== Class Definitions

include::{uml_export_dir}/classes/s_composition.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_event_context.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_content_item.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_section.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_entry.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_admin_entry.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_care_entry.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_observation_single.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_observation.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_evaluation.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_instruction.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_activity.adoc[leveloffset=+1]
include::{uml_export_dir}/classes/s_action.adoc[leveloffset=+1]

=== Transformation Rules

[cols="1,2,1,1,2,1,2", options="header"]
|===
|RM class|Path|Type|SIM class|Path|Type|Conversion

|`EVENT_CONTEXT`
|`_start_time_`
|`DV_DATE_TIME`
|`S_EVENT_CONTEXT`
|`_start_time_`
|`String`
|`_as_string()_`

|`EVENT_CONTEXT`
|`_end_time_`
|`DV_DATE_TIME`
|`S_EVENT_CONTEXT`
|`_end_time_`
|`String`
|`_as_string()_`

|`EVENT_CONTEXT`
|`_other_context_`
|`ITEM_STRUCTURE`
|`S_EVENT_CONTEXT`
|`_other_context_`
|`S_CLUSTER`
|`_xxx()_`

|===
