name: Accept
in: header
description: |
  Header to indicate the expected response format.
required: true
style: simple
schema:
  enum:
    - application/json
    - application/xml
    - text/plain
    - application/openehr.wt+json
  type: string
  example: application/xml
