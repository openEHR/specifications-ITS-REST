title: IMPORTED_VERSION
type: object
allOf:
  - $ref: ../demographic/ImportedVersion.yaml
required:
  - _type
properties:
  data:
    $ref: ../demographic/Role.yaml
  item:
    $ref: ../demographic/UMOriginalVersionOfRole.yaml
x-discriminator-value: IMPORTED_VERSION
