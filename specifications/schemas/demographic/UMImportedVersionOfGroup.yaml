title: IMPORTED_VERSION
type: object
allOf:
  - $ref: ../demographic/ImportedVersion.yaml
required:
  - _type
properties:
  data:
    $ref: ../demographic/Group.yaml
  item:
    $ref: ../demographic/UMOriginalVersionOfGroup.yaml
x-discriminator-value: IMPORTED_VERSION
