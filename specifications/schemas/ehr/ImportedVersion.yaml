title: IMPORTED_VERSION
type: object
allOf:
  - $ref: ../ehr/Version.yaml
required:
  - item
properties:
  _type:
    type: string
    enum: [IMPORTED_VERSION]
  item:
    $ref: ../ehr/OriginalVersion.yaml
x-discriminator-value: IMPORTED_VERSION
