description: |
  `200 OK` is returned when the stored AQL is successfully retrieved.
headers:
  Content-Type:
    $ref: ../headers/ContentType_JSON.yaml
content:
  application/json:
    schema:
      $ref: ../schemas/query/StoredQuery.yaml
