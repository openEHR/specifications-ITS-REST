description: |
  `200 OK` is returned when the requested VERSION is successfully retrieved.
headers:
  ETag:
    $ref: ../headers/ETag_VERSION.yaml
  Location:
    $ref: ../headers/Location_VERSIONED_EHR_STATUS_VERSION.yaml
content:
  application/json:
    schema:
      $ref: ../schemas/common/UVersion.yaml
