description: |
  `200 OK` is returned when the GROUP is successfully updated and the updated resource is returned in the body when `Prefer` header value is `return=representation`.
headers:
  ETag:
    $ref: ../headers/ETag.yaml
  Location:
    $ref: ../headers/Location_GROUP.yaml
content:
  application/json:
    schema:
      $ref: ../schemas/demographic/Group.yaml
