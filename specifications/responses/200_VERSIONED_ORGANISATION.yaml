description: |
  `200 OK` is returned when the requested VERSIONED_ORGANISATION is successfully retrieved.
content:
  application/json:
    schema:
      $ref: ../schemas/demographic/VersionedOrganisation.yaml
