description: |
  `201 Created` is returned when the ROLE was created. 
  Content body is only returned when `Prefer` header has `return=representation`, otherwise only headers are returned.
headers:
  ETag:
    $ref: ../headers/ETag.yaml
  Location:
    $ref: ../headers/Location_ROLE.yaml
content:
  application/json:
    schema:
      $ref: ../schemas/demographic/Role.yaml
