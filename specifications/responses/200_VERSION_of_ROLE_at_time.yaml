description: |
  `200 OK` is returned when the requested VERSION is successfully retrieved.
headers:
  ETag:
    $ref: ../headers/ETag_VERSION.yaml
  Location:
    $ref: ../headers/Location_VERSIONED_ROLE_VERSION.yaml
content:
  application/json:
    schema:
      $ref: ../schemas/demographic/UVersionOfRole.yaml
