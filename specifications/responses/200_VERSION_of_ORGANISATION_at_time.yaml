description: |
  `200 OK` is returned when the requested VERSION is successfully retrieved.
headers:
  ETag:
    $ref: ../headers/ETag_VERSION.yaml
  Location:
    $ref: ../headers/Location_VERSIONED_ORGANISATION_VERSION.yaml
content:
  application/json:
    schema:
      $ref: ../schemas/demographic/UVersionOfOrganisation.yaml
