description: |
  `200 OK` is returned when the requested ITEM_TAG list is successfully retrieved.
content:
  application/json:
    schema:
      type: array
      items:
        $ref: ../schemas/demographic/ItemTagOfRole.yaml
